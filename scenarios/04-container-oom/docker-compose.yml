services:
  app:
    build: .
    container_name: incident-04
    # Лимит памяти контейнера. Приложению нужно ~120 МБ, а тут задано всего
    # 50 МБ, поэтому ядро убивает процесс OOM-киллером (exit 137).
    # В этом и баг: лимит меньше, чем реально требуется приложению.
    mem_limit: 50m
