services:
  app:
    build: .
    container_name: incident-03
    # Намеренно НЕ ставим restart-политику: хотим показать, что контейнер
    # просто выходит с кодом 0 и остаётся в статусе Exited (0), а не крутится.
